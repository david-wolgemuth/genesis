[package]
name = "genesis"
version = "0.1.0"
edition = "2021"
description = "An emergent simulation of alien chemistry and life"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
rand = "0.8"
clap = { version = "4", features = ["derive"] }
